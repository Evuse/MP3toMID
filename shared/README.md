# Shared contracts

The OpenAPI schema exposed by the backend is the source of truth. Generated TypeScript
clients and versioned event schemas will live here once project processing endpoints are
introduced, avoiding hand-maintained duplicate contracts.
